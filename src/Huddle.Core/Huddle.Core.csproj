<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!-- Not published standalone: bundled into Huddle.Server / Huddle.Client / Huddle.PeerToPeer via IncludeProjectReferenceOutputsInPackage. -->

  <ItemGroup>
    <InternalsVisibleTo Include="Huddle.Server" />
    <InternalsVisibleTo Include="Huddle.Client" />
    <InternalsVisibleTo Include="Huddle.PeerToPeer" />
    <InternalsVisibleTo Include="Huddle.Core.Tests" />
  </ItemGroup>

</Project>
